# S2-PROGRESS — timestamped, per shipped improvement (commit + user-visible diff)

- **SHIP #1 — 16:2x — PROD OUTAGE RESOLVED.** Landed S1 branch to main (`280eef1..02538ae`), CI deploy `30469616040` succeeded (10m). Verified live: `pymasters.net/dashboard/classroom` now loads the full lesson list, 0 console errors — was "Something went wrong / GraduationCap is not defined" for every user since 2026-07-28. **User-visible diff: the entire Classroom (the product's core) went from crashed to working in production.** Same deploy also shipped: anon "Get Started/Start Free" → signup chooser (was sign-in); classroom hint icons (emoji→Lightbulb); org-curriculum failure clarity; output-panel AA contrast; legal-page logo unified with brand; pricing copy corrected (25 tracks/340+ lessons); org-compete empty state.

## Verification-protocol note (important for this session)
The CDP-automated tab reports `document.hidden: true` / `hasFocus: false`, which throttles requestAnimationFrame → framer-motion entrance animations (`initial opacity:0 → animate opacity:1`) FREEZE mid-flight in screenshots (e.g. dashboard stat cards measured at opacity 0.13). This is an AUTOMATION ARTIFACT, not a user-facing bug — real users with a focused tab see them settle in ~0.5s. Confirmed by JS: after any interaction (scroll nudge) the animation completes and content is crisp. Protocol: navigate → 1-tick scroll nudge → screenshot, and confirm via computed opacity, not raw post-load paint. Avoided "fixing" a phantom blank-dashboard.

- **SHIP #2 — 16:55 — "Next lesson" CTA in classroom (commit `baf29c4`).** Completing a lesson used to dump the learner back on the track list. Now the success screen shows "Up next: <title>" + a primary "Next lesson →" button that loads the next lesson in sequence and lands at the top. Verified end-to-end in browser (variables_intro → For Loops, scroll reset, 0 errors). Merged to main, deploy `30472983096` in flight. **User-visible diff: a continuous learning flow — finish → next, one tap — instead of finish → hunt.**

- **SHIP #3 — 17:1x — per-track progress in classroom list (commit `d604ce0`, merged in `85f5130`).** Track headers now show "X of Y done" + a mini progress bar and an "X/Y" (or "✓ Done") badge, driven by the already-loaded completedLessons set. Verified in browser (Python Fundamentals "1 of 36 done", Vibe Coding "0/14"). Deploy in flight. **User-visible diff: learners see their progress through every track at a glance instead of a bare lesson count.**
