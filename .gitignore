__pycache__/
*.py[cod]
.env
.streamlit/secrets.toml
.idea/
.vscode/
*.db
*.duckdb
.venv
generated/
.pymasters_localdb/
backend/vaathiyaar/Modelfile.generated
*.jsonl
**/__pycache__/
.venv/
.env*
*.pyc
.superpowers/
.claude/
# ...but DO track example templates
!.env.example
!**/.env.example

# Terraform
infra/terraform/.terraform/
infra/terraform/*.tfstate*
infra/terraform/terraform.tfvars

# Local audit/scratch artifacts (kept on disk, not committed)
_claude_audit/
_archive/
_*.py
# Stray temp-path writes (Windows paths with drive letters captured as filenames)
C:Users*

# SQLite WAL sidecars
*.db-wal
*.db-shm

# FUSE/cloud-sync half-written sidecars. Their presence means the repo lives on a
# FUSE or cloud-sync mount (Google Drive / rclone / gcsfuse) — the likely source of
# the intermittent "database is locked" conditions the code works around.
.fuse_hidden*

# Legacy Streamlit static frontend (superseded by the React app in frontend/).
static/

# LinkedIn OAuth token cache (secrets)
pipeline/.linkedin_tokens.json

# YouTube OAuth token cache (secrets)
pipeline/.youtube_token.json

# Service account / credential keys — never commit
key.json
*-key.json
*-credentials.json
*service-account*.json
gcp-*.json

# Local production-data backups (machine-first storage) — never commit user data
backups/
*.db

# Generated reports
TRANSLATION_COVERAGE_REPORT.md
