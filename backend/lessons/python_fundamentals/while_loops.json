{
  "id": "while_loops",
  "topic": "while_loops",
  "track": "python_fundamentals",
  "module": "control_flow",
  "order": 3,
  "title": { "en": "While Loops: The Night Watchman" },
  "description": { "en": "Learn while loops by following a night watchman who patrols until dawn, using break and continue to handle special events." },
  "xp_reward": 50,
  "next_unlock": "comprehensions",
  "story_variants": {
    "en": "## The Night Watchman's Patrol\n\nImagine a **night watchman** who keeps walking his beat *as long as* it's dark outside. He doesn't count rounds — he just checks: is it still night? If yes, keep going.\n\n### How a While Loop Works\n- Check the condition\n- If `True`, execute the body\n- Check again — repeat until `False`\n- If `False` from the start, the body never runs\n\n### The Syntax\n\n| Keyword | Role | Example |\n|---------|------|---------|\n| `while` | loop header | `while count < 5:` |\n| condition | keep-going test | `count < 5` |\n| `break` | exit immediately | stop if alarm rings |\n| `continue` | skip this round | skip a locked sector |\n\n> **Warning:** If the condition never becomes `False`, you get an **infinite loop**. Always make sure something inside the loop changes the condition!\n\n### Common Patterns\n- `while True:` with a `break` inside — sentinel loop\n- Counting down with `while n > 0: n -= 1`\n- Reading input until valid: `while not valid:`\n- `continue` to skip unwanted iterations without breaking"
  },
  "animation_sequence": [
    { "type": "StoryCard", "id": "intro", "content": "story_variant", "duration_ms": 4000 },
    {
      "type": "CodeStepper",
      "props": {
        "code": "count = 0\nwhile count < 3:\n    print(f'Round {count}')\n    count += 1\nprint('Dawn! Patrol over.')",
        "language": "python"
      }
    },
    {
      "type": "FlowArrow",
      "id": "while_flow",
      "label": { "en": "loop back if count < 3" },
      "direction": "up",
      "branch": "loop"
    },
    {
      "type": "TerminalOutput",
      "id": "output",
      "lines": ["Round 0", "Round 1", "Round 2", "Dawn! Patrol over."]
    },
    {
      "type": "CodeStepper",
      "props": {
        "code": "# break and continue\nfor i in range(10):\n    if i == 3:\n        continue   # skip 3\n    if i == 6:\n        break      # stop at 6\n    print(i)",
        "language": "python"
      }
    },
    { "type": "ParticleEffect", "id": "finish", "effect": "confetti", "duration_ms": 1500 }
  ],
  "practice_challenges": [
    {
      "instruction": { "en": "Write a while loop that prints numbers 1 through 5 then stops." },
      "expected_output": "1\n2\n3\n4\n5",
      "hints": [
        { "en": "Start with n = 1" },
        { "en": "Condition: while n <= 5" },
        { "en": "Don't forget to increment n inside the loop or it will run forever" }
      ],
      "test_code": "n = 1\nresult = []\nwhile n <= 5:\n    result.append(n)\n    n += 1\nassert result == [1, 2, 3, 4, 5]"
    },
    {
      "instruction": { "en": "Use a while loop with break to stop when you find the number 7 in a list." },
      "expected_output": "Found 7",
      "hints": [
        { "en": "Iterate with an index variable" },
        { "en": "Use break when the element equals 7" },
        { "en": "Print 'Found 7' before breaking" }
      ],
      "test_code": "nums = [1, 3, 7, 9]\nfound = False\ni = 0\nwhile i < len(nums):\n    if nums[i] == 7:\n        found = True\n        break\n    i += 1\nassert found == True"
    }
  ],
  "generated": false,
  "speed_multiplier": 1.0
}
