{
  "id": "semantic_search_minilm",
  "topic": "semantic_search_minilm",
  "track": "ai_engineering",
  "module": "trending_ai",
  "order": 99,
  "title": {
    "en": "Semantic Search with all-MiniLM-L6-v2"
  },
  "description": {
    "en": "Learn how the lightweight all-MiniLM-L6-v2 model turns sentences into 384-dimensional embeddings so you can build fast semantic search and RAG retrieval that understands meaning, not just keywords."
  },
  "xp_reward": 65,
  "next_unlock": null,
  "story_variants": {
    "en": "## The Day Keywords Stopped Being Enough\n\nVaathiyaar leans back and smiles. \"Imagine a student searches your notes for *'how to make my code faster'* â€” but your best note is titled *'optimizing Python loops.'* No shared keywords. Old search fails. The student leaves empty-handed.\" He taps the table. \"Today we fix that forever. We teach the machine to understand **meaning**.\"\n\nThe tool for the job is `all-MiniLM-L6-v2` â€” a sentence-transformer small enough to run on your laptop, yet powerful enough to power real semantic search. It maps any sentence into a **384-dimensional vector** (an *embedding*). Sentences that mean similar things end up close together in that space. Search becomes geometry.\n\n### Why This Model?\n\n| Property | all-MiniLM-L6-v2 | A large LLM embedder |\n|---|---|---|\n| Embedding size | 384 dims | 1536-3072 dims |\n| Speed | ~Thousands/sec on CPU | Slow / API-bound |\n| Size on disk | ~80 MB | GBs / remote only |\n| Cost | Free, local | Often paid per call |\n| Best for | RAG, search, clustering | Max accuracy tasks |\n\nIt is the *default lightweight choice* for local RAG â€” fast, free, and good enough that most users never feel the difference.\n\n### The Core Idea: Cosine Similarity\n\nTwo embeddings are compared by the **angle** between them, not their length. A cosine similarity of `1.0` means identical meaning; `0.0` means unrelated. This is why we usually `normalize_embeddings=True` â€” then a simple dot product *is* the cosine similarity.\n\n```python\nfrom sentence_transformers import SentenceTransformer, util\n\nmodel = SentenceTransformer(\"all-MiniLM-L6-v2\")\n\nquery = \"How do I make my code faster?\"\ndocs = [\n    \"A guide to optimizing Python loops\",\n    \"Best pizza recipes in Italy\",\n    \"Speeding up programs with caching\",\n]\n\nq_emb = model.encode(query, normalize_embeddings=True)\nd_emb = model.encode(docs, normalize_embeddings=True)\n\nscores = util.cos_sim(q_emb, d_emb)[0]\nfor doc, score in sorted(zip(docs, scores), key=lambda x: -x[1]):\n    print(f\"{score:.3f}  {doc}\")\n```\n\nThe loops and caching notes rise to the top; the pizza note sinks â€” even though *zero* keywords matched the query. That is the magic.\n\n### Building a Tiny Search Index\n\nFor real use you encode all your documents **once**, store the vectors, and only encode the query at search time:\n\n```python\nimport numpy as np\n\ncorpus_emb = model.encode(docs, normalize_embeddings=True)\n\ndef search(q, k=2):\n    qe = model.encode(q, normalize_embeddings=True)\n    sims = corpus_emb @ qe          # dot product = cosine\n    top = np.argsort(-sims)[:k]\n    return [(docs[i], float(sims[i])) for i in top]\n```\n\nThis is exactly the heart of a RAG pipeline: embed â†’ store â†’ retrieve the most relevant chunks â†’ feed them to an LLM.\n\n> **Key Insight:** Embeddings turn *language understanding* into *vector math*. With `all-MiniLM-L6-v2` you get meaning-aware search for free, on your own machine â€” the foundation every RAG system is built on."
  },
  "animation_sequence": [
    {
      "type": "StoryCard",
      "id": "intro",
      "title": "From Keywords to Meaning",
      "body": "Old search matches words. Semantic search matches *meaning*. Watch how all-MiniLM-L6-v2 turns sentences into vectors you can compare with simple math.",
      "emoji": "ðŸ§­"
    },
    {
      "type": "CodeStepper",
      "id": "build_semantic_search",
      "title": "Build a Semantic Search Engine",
      "language": "python",
      "steps": [
        {
          "code": "from sentence_transformers import SentenceTransformer, util",
          "explanation": "Import SentenceTransformer (the model wrapper) and util (helper functions like cosine similarity)."
        },
        {
          "code": "model = SentenceTransformer(\"all-MiniLM-L6-v2\")",
          "explanation": "Download and load the model once. It's only ~80 MB and runs comfortably on CPU."
        },
        {
          "code": "docs = [\"Optimizing Python loops\", \"Best pizza in Italy\", \"Speeding up code with caching\"]",
          "explanation": "Our tiny corpus â€” the documents we want to search through."
        },
        {
          "code": "doc_emb = model.encode(docs, normalize_embeddings=True)",
          "explanation": "Encode every document into a 384-dim vector. normalize_embeddings=True makes dot product equal cosine similarity."
        },
        {
          "code": "print(doc_emb.shape)  # (3, 384)",
          "explanation": "Three documents, each a 384-dimensional embedding. This is the shape of meaning."
        },
        {
          "code": "query = \"How do I make my code faster?\"",
          "explanation": "A natural-language question that shares NO keywords with the best-matching document."
        },
        {
          "code": "q_emb = model.encode(query, normalize_embeddings=True)",
          "explanation": "Encode the query into the same 384-dim space so we can compare it to the documents."
        },
        {
          "code": "scores = util.cos_sim(q_emb, doc_emb)[0]",
          "explanation": "Compute cosine similarity between the query and each document. Higher = closer in meaning."
        },
        {
          "code": "best = docs[int(scores.argmax())]",
          "explanation": "Pick the document with the highest similarity â€” the most semantically relevant match."
        },
        {
          "code": "print(best)  # Speeding up code with caching",
          "explanation": "Meaning wins over keywords. The caching/loops notes beat the pizza note despite zero word overlap."
        }
      ]
    },
    {
      "type": "ParticleEffect",
      "id": "finish",
      "title": "You Built Semantic Search!",
      "body": "You now understand embeddings, cosine similarity, and the retrieval core of every RAG system.",
      "effect": "confetti",
      "emoji": "âœ¨"
    }
  ],
  "practice_challenges": [
    {
      "instruction": {
        "en": "Complete the `embed_sentence` function so it returns a normalized 384-dimensional embedding for the given text using all-MiniLM-L6-v2. The model is already loaded as `model`."
      },
      "starter_code": "from sentence_transformers import SentenceTransformer\n\nmodel = SentenceTransformer(\"all-MiniLM-L6-v2\")\n\ndef embed_sentence(text):\n    # Return a normalized embedding vector for `text`\n    pass\n\nemb = embed_sentence(\"Semantic search is powerful\")\nprint(len(emb))",
      "expected_output": "384",
      "hints": {
        "en": [
          "Use model.encode(text) to get the embedding.",
          "Pass normalize_embeddings=True so the vector has unit length.",
          "The MiniLM model always returns 384 dimensions â€” use len() or .shape to check."
        ]
      },
      "test_code": "import numpy as np\nemb = embed_sentence(\"Semantic search is powerful\")\nassert len(emb) == 384, \"Embedding must have 384 dimensions\"\nnorm = float(np.linalg.norm(emb))\nassert abs(norm - 1.0) < 1e-3, \"Embedding should be normalized to unit length\"\nprint(\"PASS\")"
    },
    {
      "instruction": {
        "en": "Implement `most_similar` which, given a query and a list of documents, returns the single document whose meaning is closest to the query. Use cosine similarity on all-MiniLM-L6-v2 embeddings."
      },
      "starter_code": "from sentence_transformers import SentenceTransformer, util\n\nmodel = SentenceTransformer(\"all-MiniLM-L6-v2\")\n\ndef most_similar(query, documents):\n    # Return the document most semantically similar to `query`\n    pass\n\ndocs = [\"A guide to optimizing Python loops\",\n        \"Best pizza recipes in Italy\",\n        \"Speeding up programs with caching\"]\nprint(most_similar(\"How do I make my code faster?\", docs))",
      "expected_output": "Speeding up programs with caching",
      "hints": {
        "en": [
          "Encode the query and all documents with normalize_embeddings=True.",
          "Use util.cos_sim(query_emb, doc_emb) to get a similarity score per document.",
          "Use argmax() on the scores to find the index of the best matching document."
        ]
      },
      "test_code": "docs = [\"A guide to optimizing Python loops\",\n        \"Best pizza recipes in Italy\",\n        \"Speeding up programs with caching\"]\nresult = most_similar(\"How do I make my code faster?\", docs)\nassert result == \"Speeding up programs with caching\", f\"Got: {result}\"\nfood = most_similar(\"What should I eat for dinner?\", docs)\nassert food == \"Best pizza recipes in Italy\", f\"Got: {food}\"\nprint(\"PASS\")"
    }
  ],
  "tags": {
    "concepts_taught": [
      "sentence embeddings",
      "semantic search",
      "cosine similarity",
      "vector representations",
      "sentence-transformers",
      "RAG retrieval"
    ],
    "concepts_required": [
      "python functions",
      "lists",
      "basic numpy"
    ],
    "difficulty": "intermediate",
    "engagement_type": "code_along",
    "estimated_minutes": 20,
    "category": "nlp_embeddings",
    "path_memberships": [
      "ai_engineering",
      "trending_ai",
      "rag_fundamentals"
    ]
  },
  "generated": true
}
