{
  "id": "prompt_caching_breakeven",
  "topic": "prompt_caching_breakeven",
  "track": "ai_engineering",
  "module": "trending_ai",
  "order": 109,
  "title": {
    "en": "Prompt Caching: How Many Reuses Until It Pays Off?"
  },
  "description": {
    "en": "Writing a prompt cache isn't free — it costs more than a normal call. So when does paying that premium actually save money? Compute the exact breakeven point yourself."
  },
  "xp_reward": 75,
  "next_unlock": null,
  "story_variants": {
    "en": "## Caching Has A Cover Charge\n\n\"A cache hit is 90% cheaper,\" Vaathiyaar says, \"but somebody has to build the cache first, and *that* first call isn't discounted — it's marked up. Writing a prompt cache costs **1.25x** the normal input rate for a short-lived (5-minute) cache, or **2x** for a longer-lived (1-hour) cache. Skip a step here and you can end up paying *more*, not less.\"\n\n### One Write, Then However Many Reads\n\nA realistic usage pattern is: call #1 writes the cache (at the write multiplier), and calls #2, #3, ... read it (at the 0.1x hit multiplier), until the cache expires. Compare that against never caching at all, where every call pays the full 1.0x rate:\n\n```\nn calls, no cache:     n x 1.0\nn calls, with cache:   1 x write_multiplier + (n - 1) x 0.1\n```\n\nFor a 5-minute cache (`write = 1.25`), the two lines cross fast: even a *single* follow-up read already wins, because `1.25 + 0.1 = 1.35`, which beats two uncached calls at `2 x 1.0 = 2.0`. For a 1-hour cache (`write = 2.0`), you need a couple more reads before the premium pays for itself.\n\n### Why This Matters More As Caches Get Longer-Lived\n\nA longer TTL (1 hour vs. 5 minutes) survives more idle time between calls without expiring — but it costs more up front, so it only wins if the *same* prefix genuinely gets reused enough times within that window. Picking the wrong TTL for your traffic pattern quietly turns a cost optimization into a cost increase.\n\n> **Vaathiyaar's rule:** never assume caching saves money — compute the breakeven point for your actual call pattern and TTL, then decide.\n\nToday you compute exactly how many reads it takes for each cache duration to pay for itself."
  },
  "animation_sequence": [
    {
      "type": "StoryCard",
      "id": "intro",
      "content": "story_variant",
      "visual_theme": "cache_write_premium",
      "duration_ms": 4000
    },
    {
      "type": "ConceptMap",
      "id": "overview",
      "nodes": [
        {
          "label": "Cache breakeven",
          "children": [
            "cache write costs a premium (1.25x or 2x)",
            "cache read costs 0.1x",
            "no-cache cost = n x 1.0",
            "find smallest n where cached total < no-cache total"
          ]
        }
      ],
      "duration_ms": 3000
    },
    {
      "type": "CodeStepper",
      "id": "calc",
      "steps": [
        {
          "line": "for n in range(1, max_calls + 1):",
          "highlight": "for n in range",
          "explanation": {
            "en": "Try increasing numbers of total calls, starting from 1"
          }
        },
        {
          "line": "cached_total = write_multiplier + (n - 1) * 0.1",
          "highlight": "write_multiplier + (n - 1) * 0.1",
          "explanation": {
            "en": "Call 1 writes the cache; calls 2..n read it at 0.1x"
          }
        },
        {
          "line": "if cached_total < n: return n",
          "highlight": "cached_total < n",
          "explanation": {
            "en": "The no-cache cost for n calls is just n (each at 1.0x); stop at the first n where caching wins"
          }
        }
      ],
      "duration_ms": 5000
    },
    {
      "type": "ComparisonPanel",
      "id": "cmp",
      "left": {
        "title": "5-minute cache (write 1.25x)",
        "items": [
          "n=1: 1.25 vs 1.0 -> caching loses",
          "n=2: 1.35 vs 2.0 -> caching wins",
          "breakeven at 2 calls"
        ]
      },
      "right": {
        "title": "1-hour cache (write 2.0x)",
        "items": [
          "n=2: 2.10 vs 2.0 -> caching loses",
          "n=3: 2.20 vs 3.0 -> caching wins",
          "breakeven at 3 calls"
        ]
      },
      "duration_ms": 4000
    },
    {
      "type": "TerminalOutput",
      "id": "term",
      "lines": [
        "cache_breakeven_calls(1.25) -> 2",
        "cache_breakeven_calls(2.0) -> 3"
      ],
      "duration_ms": 4000
    },
    {
      "type": "ParticleEffect",
      "id": "celebrate",
      "theme": "breakeven_found",
      "duration_ms": 2000
    }
  ],
  "practice_challenges": [
    {
      "instruction": {
        "en": "Implement `cache_breakeven_calls(write_multiplier, max_calls=20)` — the smallest total number of calls `n` (a write, then `n - 1` reads) at which caching costs less than never caching. For each `n` from 1 up to `max_calls`, compute `cached_total = write_multiplier + (n - 1) * 0.1` and compare it to the no-cache total of `n` (n calls at 1.0x each). Return the first `n` where `cached_total < n`. Then print `cache_breakeven_calls(1.25)` (expected: `2`)."
      },
      "starter_code": "def cache_breakeven_calls(write_multiplier: float, max_calls: int = 20) -> int:\n    \"\"\"Smallest n (1 cache write + n-1 reads) where caching beats paying full price every call.\"\"\"\n    for n in range(1, max_calls + 1):\n        cached_total = write_multiplier + (n - 1) * 0.1\n        # if cached_total < n: return n\n        pass\n    return -1\n\nprint(cache_breakeven_calls(1.25))",
      "hints": {
        "en": [
          "no_cache_total for n calls is simply n (each call is 1.0x)",
          "cached_total = write_multiplier + (n - 1) * 0.1",
          "Return n the first time cached_total < n"
        ]
      },
      "expected_output": "2"
    },
    {
      "instruction": {
        "en": "Reuse `cache_breakeven_calls` to compare cache durations. Print the breakeven call count for a 5-minute cache (`write_multiplier=1.25`) and for a 1-hour cache (`write_multiplier=2.0`), one per line, in that order."
      },
      "starter_code": "def cache_breakeven_calls(write_multiplier: float, max_calls: int = 20) -> int:\n    for n in range(1, max_calls + 1):\n        cached_total = write_multiplier + (n - 1) * 0.1\n        if cached_total < n:\n            return n\n    return -1\n\n# print the breakeven count for a 5-minute cache (1.25x), then a 1-hour cache (2.0x)\nprint(cache_breakeven_calls(1.25))\nprint(cache_breakeven_calls(2.0))",
      "hints": {
        "en": [
          "Call cache_breakeven_calls once with 1.25, once with 2.0",
          "Print each result on its own line, 5-minute cache first"
        ]
      },
      "expected_output": "2\n3"
    }
  ],
  "tags": {
    "concepts_taught": [
      "prompt_caching",
      "cost_optimization",
      "breakeven_analysis",
      "llm_api_pricing"
    ],
    "concepts_required": [
      "python_functions",
      "loops",
      "arithmetic"
    ],
    "difficulty": "intermediate",
    "engagement_type": "hands_on",
    "estimated_minutes": 25,
    "category": "ai_engineering",
    "path_memberships": [
      "ai_engineering",
      "trending_ai"
    ]
  },
  "generated": false
}
