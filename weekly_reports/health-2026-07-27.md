# PyMasters Weekly Health Report — 2026-07-27

**Overall Status: PARTIAL ⚠️** — Backend tests all pass; live-site smoke tests and institutional-console regression blocked by egress policy.

---

## Executive Summary

| Area | Result | Detail |
|------|--------|--------|
| Live Smoke Tests (Step 1) | ⛔ BLOCKED | Egress proxy denies `pymasters-977064896391.us-central1.run.app` |
| Institutional Console Regression (Step 1b) | ⛔ BLOCKED | Requires live site access |
| Backend Unit Tests (Step 2) | ✅ PASS | 328 passed, 0 failed, 1 warning |
| UX & Trends Review (Step 3) | ✅ DONE | 3 improvements identified |

---

## Step 1 — End-to-End Smoke Tests

**Status: BLOCKED — 0/N checks completed**

All outbound HTTPS to `pymasters-977064896391.us-central1.run.app` is denied by the session's organisation egress policy. Both `curl` (via the CONNECT proxy at `127.0.0.1:46513`) and the `WebFetch` tool returned HTTP 403 from the proxy gateway, with the policy-denial detail:

```
"gateway answered 403 to CONNECT (policy denial or upstream failure)"
host: "pymasters-977064896391.us-central1.run.app:443"
```

This is a network policy configuration issue in the CI/scheduled-task environment, **not** evidence that the live site is down. All checks below could not be run:

| Check | Expected | Result |
|-------|----------|--------|
| GET /health | 200 | ⛔ BLOCKED |
| GET / | 200 HTML | ⛔ BLOCKED |
| POST /api/auth/register | 200 + id | ⛔ BLOCKED |
| POST /api/auth/login | 200 + token | ⛔ BLOCKED |
| GET /api/paths/ | 200 non-empty | ⛔ BLOCKED |
| GET /api/trending/ | 200 non-empty | ⛔ BLOCKED |
| GET /api/challenges/ | 200 non-empty | ⛔ BLOCKED |
| GET /api/reference/ | 200 non-empty | ⛔ BLOCKED |
| GET /api/graph/ | 200 non-empty | ⛔ BLOCKED |
| GET /api/classroom/ (modules) | 200 non-empty | ⛔ BLOCKED |
| POST /api/modules/request (AI gen) | job_id | ⛔ BLOCKED |
| GET /api/modules/status/{job_id} | progress | ⛔ BLOCKED |

**Recommendation:** Allow the scheduled-task environment to reach Cloud Run (whitelist the `.run.app` hostname) or run smoke tests from a GCP-internal service account that has VPC/IAM access.

---

## Step 1b — Institutional Console Regression

**Status: BLOCKED — 0/12 checks completed**

Depends on live site access; all checks could not execute.

| Check | Expected | Result |
|-------|----------|--------|
| POST /api/org (create org) | 200 + ORG_ID | ⛔ BLOCKED |
| GET /api/org/{ORG_ID}/groups | 200, groups + group_label | ⛔ BLOCKED |
| PUT /api/org/{ORG_ID}/members/{UID}/groups | 200, groups:["QA-Class"] | ⛔ BLOCKED |
| GET /api/org/{ORG_ID}/groups (after tag) | QA-Class present, count 1 | ⛔ BLOCKED |
| GET /api/org/{ORG_ID}/progress?group=QA-Class | UID visible, groups has QA-Class | ⛔ BLOCKED |
| GET /api/org/{ORG_ID}/progress?group=__ungrouped__ | UID absent | ⛔ BLOCKED |
| GET /api/org/{ORG_ID}/students/{UID} | 200, profile+summary+mastery+activity+lessons | ⛔ BLOCKED |
| PUT /api/org/{ORG_ID} (rename label) | 200 | ⛔ BLOCKED |
| GET /api/org/{ORG_ID}/groups (after rename) | group_label == "Class" | ⛔ BLOCKED |
| GET /api/org/{ORG_ID}/groups — no auth | 401 | ⛔ BLOCKED |
| PUT .../members/{UID}/groups — no auth | 401 | ⛔ BLOCKED |
| DELETE /api/org/{ORG_ID} (cleanup) | 200 | ⛔ BLOCKED |

---

## Step 2 — Backend Tests

**Status: ✅ PASS — 328/328**

```
328 passed, 1 warning in 168.97s (0:02:48)
```

- **Root cause of initial errors:** System `python-jose` package linked against a missing `_cffi_backend`. Fixed by installing `cffi` via pip, which restored the correct backend. All tests then collected and passed cleanly.
- **1 warning:** `starlette.testclient` deprecation — `httpx2` not installed. Non-blocking; cosmetic.

Test files covered (38 modules):

`test_access`, `test_admin_seed`, `test_ai_resilience`, `test_auth_hardening`, `test_auth_security`, `test_challenge_archive`, `test_classroom`, `test_classroom_graceful`, `test_engine_parsing`, `test_enterprise_endpoint_idor`, `test_enterprise_tracks`, `test_execution`, `test_grading_placeholders`, `test_graph_auth`, `test_install_package_noop`, `test_language`, `test_lesson_catalog`, `test_lesson_loader`, `test_lesson_solvability`, `test_module_generation_db`, `test_notification_dispatcher`, `test_notifications_api`, `test_notifications_db`, `test_ondemand_translate_guard`, `test_org_datetime`, `test_password_hashing`, `test_platform_settings`, `test_playground_signal_capture`, `test_profile`, `test_qubrid_provider`, `test_ratelimit`, `test_register`, `test_sandbox_egress`, `test_seed_links`, `test_self_serve_enrollment`, `test_semantic`, `test_struggle_feedback`, `test_super_admin_authz`, `test_support`, `test_telemetry`, `test_translation_fallback_flag`, `test_translator`, `test_trigger_engine`, `test_tutor_sessions`, `test_vaathiyaar_engine`

---

## Step 3 — UX & Trends

### Observed Issues (from code review)

- No user-facing regressions visible from static code review; all unit tests pass.
- The `httpx` / `httpx2` deprecation warning means the test harness is running on a slightly stale stack but this does not affect production.

### 2026 AI-Tutor / EdTech UX Trends

**Sources:**
- [EdTech Trends in 2026 — TCS](https://www.tcs.com/what-we-do/industries/education/article/edtech-trends-2026-intelligence-redefining-learning-systems)
- [Why UX Matters as Much as Algorithm Accuracy — StudyMonkey](https://studymonkey.ai/blog/why-ux-matters-as-much-as-algorithm-accuracy-in-ai-tutoring-platform-design)
- [AI Tutoring Platforms: What Works in 2026 — Datasofttechnologies](https://datasofttechnologies.com/blog/ai-tutoring-platforms-for-edtech-smes-in-2026-what-actually-improves-student-outcomes)

### Top 3 Prioritised Improvement Ideas

| Priority | Improvement | Rationale |
|----------|-------------|-----------|
| **P1 — High** | **Real-time adaptive pacing on lessons** — adjust difficulty and next-step hints dynamically based on per-student error patterns, not just module completion. | 2026 research shows RL-DKT reduces dropout 50% and improves task completion 12.5%. PyMasters already tracks struggle feedback; surfacing it in the lesson UX is the missing layer. |
| **P2 — Medium** | **Contextual AI explanation on wrong answers** — when a quiz answer is wrong, trigger an inline Ollama explanation targeted at the misconception, not just "incorrect". | "Most AI tutoring products are good at conversation but bad at instruction" (2026 industry finding). The module-generation pipeline already exists; re-use it for micro-explanations. |
| **P3 — Medium** | **Instructor dashboard nudges for at-risk students** — in the institutional console, surface a red/amber/green status per student based on streak, last-active, and mastery gap; send automated nudge emails. | Navigation friction between group progress view and per-student drill-down is the current bottleneck. Proactive nudges move resolution before students drop off. |

---

## Action Items

1. **Urgent:** Configure the scheduled-task environment to allow egress to `*.run.app` so smoke tests can execute next week.
2. **Fix:** Pin `httpx2` (or equivalent) in `backend/requirements.txt` to clear the starlette deprecation warning.
3. **Consider:** Add `cffi` explicitly to `requirements.txt` (currently transitive only via `python-jose`) to prevent the test-environment breakage seen at the start of this run.

---

*Generated by PyMasters Weekly QA Agent — 2026-07-27*
